<%- include("../partials/header") %>

<h1><%= locals.title %></h1>

<div>
  <% products.forEach(product => { %>
    <div>
      <%- product.name %>
    </div>
  <% }); %>
</div>

<%- include("../partials/footer") %>