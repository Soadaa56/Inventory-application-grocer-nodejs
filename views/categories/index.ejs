<%- include("../partials/header") %>

<h1><%= locals.title %></h1>

<div>
  <% categories.forEach(category => { %>
    <div>
      <%- category.name %>
    </div>
  <% }); %>
</div>

<%- include("../partials/footer") %>